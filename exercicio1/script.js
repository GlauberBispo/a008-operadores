let checarPar = Number(prompt('Insira um número par.'));
console.log('resto da divisão:', (checarPar %= 2));

// Atribuindo números pares para a variável checarPar o resto da divisão sempré é 0;
// Qundo atribuido um número impar o resto da divisão é ígual a 1;
